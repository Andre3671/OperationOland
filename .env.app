# Loaded by `vite build --mode app` (npm run build:app / android:sync).
# Marks the build as the NATIVE APP build: player UI only, no admin routes.
# The web build (`npm run build`) never loads this file.
VITE_APP_MODE=app
