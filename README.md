# Operation Öland — Vite + Vue 3

Mobile-first web app for a gamified roadtrip challenge.

Quick start:

Install and run:

```
npm install
npm run dev
```

Build:

```
npm run build
docker build -t operation-oland .
docker run -p 8080:80 operation-oland
```
